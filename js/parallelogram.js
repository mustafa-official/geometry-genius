function calculateParallelogram() {
    // get parallelBase
    const parallelBase = document.getElementById('parallel-base').value;
    const base = parseFloat(parallelBase);

    // get parallelHeight
    const parallelHeight = document.getElementById('parallel-height').value;
    const height = parseFloat(parallelHeight);
    console.log(height);

    // get result 
    


}
